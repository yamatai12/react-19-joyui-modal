import * as React from "react";
import {
  Modal,
  ModalDialog,
  ModalClose,
  DialogTitle,
  DialogContent,
} from "@mui/joy";

export default function App() {
  return (
    <Modal open={true}>
      <div>sample</div>
    </Modal>
  );
}
